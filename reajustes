algoritmo "reajuste"

var

sal, reajuste, nsal: real

inicio

escreva("qual o seu salario mensal? R$")
leia(sal)
escreva("de quantos % será o reajuste? ")
leia(reajuste)

nsal <- sal * (reajuste/100) + sal

escreva("seu salario com o reajuste será de R$", nsal)

fimalgoritmo
